/* `https://raw.githubusercontent.com/JohanL1bert/ImageDay/main/images/${timeDay}/${number}.jpg`; */

/* const getData = async () => {
  const response = await fetch(
    `https://raw.githubusercontent.com/JohanL1bert/art-picture/main/data.json`
  );

  if (response.ok) {
    const data = await response.json();
    return data;
  }
  throw new Error("error");
};

const filter = (dataObj) => {
  dataObj.then((data) => {
    data.map((el, index) => {
      console.log(el);
    });
  });
};

const data = getData();
filter(data); */
